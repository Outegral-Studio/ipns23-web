export const metadata = {
    title: "同學",
    description: "是誰就讀清大的原科 23",
}

export default function ClassmateLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}