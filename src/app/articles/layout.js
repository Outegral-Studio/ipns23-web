export const metadata = {
    title: "文章",
    description: "他們的文筆真的很好。尤其是歐予恩",
}

export default function ClassmateLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}